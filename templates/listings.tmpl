<header>Heading stuff
	<ul>
		<li><a href="#">Nav1</a></li>
		<li><a href="#">Nav2</a></li>
		<li><a href="#">Nav3</a></li>
	</ul>
</header>

<% _.forEach(results, function(result) { %>

	<div class="container">
		<div listing = '<%= result.listing_id %>'>
			<div class="imgBox">
				<img src = '<%= result.MainImage.url_570xN %>'>
			</div>

			<div class="title">
					<%= result.title %>
			</div>
				
			<div class="price">
					<div class="priceText">
					<%= result.price %>
					</div>
			</div>
			</div>
			</div>
	</div>

<% }); %>
	
